#copy this file to drush.yml and edit the settings below
command:
  site:
    install:
      options:
        #email address of the "admin" user
        account-mail: info@spinspire.com
        #email address of the site
        site-mail: info@spinspire.com

#global options
options:
  #replace this with output of:
  #echo "http://$(docker inspect -f '{{range.NetworkSettings.Networks}}{{.IPAddress}}{{end}}' $(docker-compose ps -q web))"
  uri: http://changeme
